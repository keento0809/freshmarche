import React from "react";
import Box from "@mui/material/Box";

const OrderSummaryList = () => {
  const orderSummaryData = localStorage.getItem("cartInfo");
  return <Box sx={{ color: "primary" }}>TEST</Box>;
};

export default OrderSummaryList;
